---
import Navbar from "@components/Navbar.astro";
import { Font } from "astro:assets";
import Analytics from "@vercel/analytics/astro";
import { SITE_CONFIG } from "@config";
import Footer from "@components/Footer.astro";
import "../styles/global.css";
import type { FooterProps, LinkProps } from "@types";

interface Props {
  title?: string;
  description?: string;
  ogTitle?: string;
  ogDescription?: string;
  ogImage?: string;
  ogType?: "article" | "profile" | "website";
  robots?: string;
  lang?: string;
  structuredData?: Record<string, unknown> | Record<string, unknown>[];
  hideFooter?: boolean;
  navLinks?: LinkProps[];
  footer?: FooterProps;
  footerLabels?: {
    navigation: string;
    contact: string;
    legal: string;
    rights: string;
  };
}

const pageTitle = Astro.props.title;
const pageDescription = Astro.props.description;
const pageOgTitle = Astro.props.ogTitle;
const pageOgDescription = Astro.props.ogDescription;
const pageOgImage = Astro.props.ogImage;
const pageOgType = Astro.props.ogType;
const pageRobots = Astro.props.robots ?? "index, follow";
const pageLang = Astro.props.lang;
const pageStructuredData = Astro.props.structuredData;
const hideFooter = Astro.props.hideFooter ?? false;
const pageNavLinks = Astro.props.navLinks;
const pageFooter = Astro.props.footer;
const pageFooterLabels = Astro.props.footerLabels;

const {
  brand,
  siteUrl,
  title: defaultTitle,
  description: defaultDescription,
  ogTitle: defaultOgTitle,
  ogDescription: defaultOgDescription,
  ogImage,
  navLinks,
  language,
  author,
  socialLinks,
  footer,
} = SITE_CONFIG;

const title = pageTitle ?? defaultTitle;
const description = pageDescription ?? defaultDescription;
const htmlLanguage = pageLang ?? language;
const resolvedNavLinks = pageNavLinks ?? navLinks;
const resolvedFooter = pageFooter ?? footer;
const ogTitle = pageOgTitle ?? pageTitle ?? defaultOgTitle;
const ogDescription =
  pageOgDescription ?? pageDescription ?? defaultOgDescription;
const canonicalURL = new URL(Astro.url.pathname, siteUrl).href;
const ogImageUrl = new URL(pageOgImage ?? ogImage, siteUrl).href;
const isHome = Astro.url.pathname === "/";
const sameAs = socialLinks.map(({ href }) => href);
const structuredData = [
  {
    "@context": "https://schema.org",
    "@type": "Person",
    name: brand,
    alternateName: ["Jesus Zuna", "Jesus Zuna Pacheco", "Jesús Zuña Pacheco"],
    url: siteUrl,
    jobTitle:
      "Desarrollador de software y estudiante de Ingeniería de Software",
    address: {
      "@type": "PostalAddress",
      addressLocality: "Guayaquil",
      addressCountry: "EC",
    },
    alumniOf: {
      "@type": "CollegeOrUniversity",
      name: "Universidad de Guayaquil",
    },
    email: SITE_CONFIG.email,
    sameAs,
  },
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: brand,
    url: siteUrl,
    description,
    inLanguage: language,
    publisher: {
      "@type": "Person",
      name: brand,
    },
  },
  ...(isHome
    ? [
        {
          "@context": "https://schema.org",
          "@type": "ProfilePage",
          name: title,
          url: canonicalURL,
          description,
          inLanguage: language,
          mainEntity: {
            "@type": "Person",
            name: brand,
            url: siteUrl,
            sameAs,
          },
        },
      ]
    : []),
  ...(pageStructuredData
    ? Array.isArray(pageStructuredData)
      ? pageStructuredData
      : [pageStructuredData]
    : []),
];
---

<!doctype html>
<html lang={htmlLanguage} class="scroll-pt-16 scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <script is:inline>
      (() => {
        const storageKey = "theme";
        const root = document.documentElement;
        const savedTheme = localStorage.getItem(storageKey);
        const systemTheme = window.matchMedia("(prefers-color-scheme: dark)")
          .matches
          ? "dark"
          : "light";
        const theme =
          savedTheme === "light" || savedTheme === "dark"
            ? savedTheme
            : systemTheme;

        root.classList.remove("light", "dark");
        root.classList.add(theme);
        root.style.colorScheme = theme;
      })();
    </script>
    <meta name="description" content={description} />
    <meta name="author" content={author} />
    <meta name="robots" content={pageRobots} />
    <meta name="viewport" content="width=device-width" />
    <meta
      name="theme-color"
      content="#f8f5ff"
      media="(prefers-color-scheme: light)"
    />
    <meta
      name="theme-color"
      content="#0d1218"
      media="(prefers-color-scheme: dark)"
    />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonicalURL} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <Font cssVariable="--font-sora" preload />
    <Font cssVariable="--font-inter" preload />

    <meta property="og:title" content={ogTitle} />
    <meta property="og:description" content={ogDescription} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:site_name" content={brand} />
    <meta
      property="og:type"
      content={pageOgType ?? (isHome ? "profile" : "website")}
    />
    <meta property="og:locale" content={htmlLanguage === "en" ? "en_US" : "es_EC"} />

    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={ogTitle} />
    <meta property="twitter:description" content={ogDescription} />
    <meta property="twitter:image" content={ogImageUrl} />
    <Analytics />
    <script
      is:inline
      type="application/ld+json"
      set:html={JSON.stringify(structuredData)}
    />
  </head>
  <body class="min-h-screen font-sans text-white">
    <Navbar brand={brand} navLinks={resolvedNavLinks} />
    <main class="mx-auto max-w-5xl px-5">
      <slot />
    </main>
    {
      !hideFooter && (
        <Footer
          author={author}
          brand={brand}
          socialLinks={socialLinks}
          footer={resolvedFooter}
          labels={pageFooterLabels}
        />
      )
    }
  </body>
</html>
